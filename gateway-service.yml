spring:

  # cloud:
  #   gateway:
  #     server:
  #       webflux:
  #         routes:
  #           - id: product
  #             uri: lb://PRODUCT
  #             predicates:
  #               - Path=/api/products/**
  #           - id: user
  #             uri: lb://USER
  #             predicates:
  #               - Path=/api/users/**
  #           - id: order
  #             uri: lb://ORDER
  #             predicates:
  #               - Path=/api/orders/**,/api/carts/**
  #           - id: eureka-server
  #             uri: http://localhost:8761
  #             predicates:
  #                 - Path=/eureka/main
  #             filters:
  #               - SetPath=/
  #           - id: eureka-server-static
  #             uri: http://localhost:8761
  #             predicates:
  #               - Path=/eureka/**

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  health:
    circuitbreakers:
      enabled: true
  tracing:
    sampling:
      probability: 1.0

