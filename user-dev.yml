spring:
  mongodb:
    uri: mongodb://localhost:27017/ecom_user
    database: userdb
logging:
  level:
    root: INFO
    com.ecommerce.user: TRACE
  file:
    name: logs/${spring.application.name}.log
  logback:
    rollingpolicy:
      max-file-size: 5MB
      max-history: 7

    
