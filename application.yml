spring:
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

    rabbitmq:
        host: localhost
        port: 15672
        username: guest
        password: guest



management:
    endpoints:
        web:
            exposure:
                include: "*"
